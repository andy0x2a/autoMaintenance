
angular.module('autoMaintenanceApp.constants', [])
    .service('constants', function () {
        return {
            config: {
                apiBase: "http://localhost:8080"

            }
        }
    });
